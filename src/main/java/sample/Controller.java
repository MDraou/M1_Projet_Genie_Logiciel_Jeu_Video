package sample;

import java.awt.event.ActionEvent;
import java.awt.event.KeyEvent;

import java.beans.EventHandler;
import java.net.MalformedURLException;

public class Controller {
/*EventHandler eventHandler;

    Controller(PlayerController player) {
        this.eventHandler = (EventHandler) event -> {
            try {
                switch () {
                    case KeyEvent.VK_UP: case Z: player.goNorth(); break;
                    case DOWN: case S: player.goSouth(); break;
                    case LEFT: case Q: player.goWest(); break;
                    case RIGHT: case D: player.goEast(); break;
                    case I: player.goInventory(); break;
                    case E: player.actionE(); break;
                    case A: player.actionA(); break;
                }
            } catch (MalformedURLException exception) { exception.printStackTrace(); }
        }; }*/
}
